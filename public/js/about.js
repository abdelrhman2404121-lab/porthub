// public/js/about.js — About page
'use strict';

document.addEventListener('DOMContentLoaded', () => {
    // About page is mostly static — just ensure auth state is reflected in navbar.
    // The initNavbar() in main.js already handles this.
    // No API calls needed for the about page.
});
